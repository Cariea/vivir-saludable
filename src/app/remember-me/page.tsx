import { Input } from "@/components/Input";

export default function RememberMe()   {
  return (
    <div>
      <Input labelValue="recibira una codigo en su corrreo electronico" textButton="remember"/>
    </div>
  );
}